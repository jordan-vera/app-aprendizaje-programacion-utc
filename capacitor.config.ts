import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-aprendizaje-programacion-utc',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
